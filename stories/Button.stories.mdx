import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Button, Props } from '../src/Button';

<Meta title="MDX/Button" component={Button} />

export const Template = (args) => <Button {...args} />;

# Button
Lets define a story for our `Button` component:

<Story name="My Button" args={{children: 'I`m in MDX'}}>
    {Template.bind({})}
</Story>

<ArgsTable story="My Button" />

I want extra text description here!!

<Canvas>
    <Story name="Secondary" args={{children: 'MDX secondary', variant: 'secondary'}}>
        {Template.bind({})}
    </Story>
</Canvas>